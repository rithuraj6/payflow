apiVersion: v1
kind: Secret
metadata:
  name: {{ include "payflow.fullname" . }}-secret
  labels:
    {{- include "payflow.labels" . | nindent 4 }}

type: Opaque

stringData:
  DATABASE_PASSWORD: {{ .Values.postgres.password | quote }}

